#!/bin/bash
cd $LFS/sources
rm -v bd_file
rm -v bd_folder
echo $1 > bd_file
FOLDER=`cat bd_file | sed -s 's/.tar.*//g'`
echo "$FOLDER" > bd_folder
tar -xvf $1
cd $FOLDER
mkdir -v build
echo -e "\t$LFS/sources/$FOLDER/build"
echo "------------- simdi config yapabilirsiniz ------"
